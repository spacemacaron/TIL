# 10월 26일 

```html
<a href="어떤링크" style="color:red"> </a> 

그 페이지에 머무르는 동안에 링크 색깔을 변경하고 싶을 때 사용.
여기서 stlyle은 속성 속성의 값을 웹 브라우저가 css 의 문법에 따라 해석한 결과를 해당 속성이 위치하고 있는 태그에 적용할 것이다~

style="" / html의 속성임. 그리고 그 값으로 반드시 css의 효과가 들어온다.  

style 태그를 직접 사용할 경우에는 태그가 위치하고 있는 태그에게 효과를 줄 것이기 때문에, '선택자'사용은 따로 필요하지 않다.

<class="saw">html 
> class 값이 saw 이것을 css에서 saw{} 이렇게 쓰면 아무 일도 일어나지 않는다.  saw라는 이름의 태그를 선택하라는 뜻이기 때문임! 
.saw{} 가 되면 class가 saw인 태그를 가리키는 선택자 class 는 그룹핑(grouping) class라고 하는 속성의 값에는 여러 값이 들어갈 수 있고, 띄어쓰기로 구분한다. 

하나의 태그에는 여러개의 속성이 들어갈 수 있다. 여러 개의 선택자를 통해서 공동으로
제어할 수 있다. 그리고, 더 가까이에 있는 명령이 더 강한 영향력을 갖는다. 
```

---

```css
<style> 
(selector)a{
  color(property:속성):black; (value:값)
  text-decoration: none; 
}
</style> 

>코드 내용 전체:선언 declaration

에서 style 이라는 태그는 저 효과를 누구에게 지정할 지 설명할 수가 없기 때문에 
a {} 라고 하는 코드가 추가적으로 필요한 것이다. 효과를 누구에게 줄 것인지를 선택한다는
의미에서 '선택자(selector)' 라고 함. 

color: black; 은 효과 (declaration)-선언

text-decoration: none; 

border-width:5px;
border-color:red;
border-style:solid;
=
border: 5px solid red; / 여기에서, 5px solid red 의 순서는 중요하지 않음 
여백을 padding 이이라고 함. 
```

---

<aside>

💡 **오늘의 추천 검색어**

</aside>

- css text size property
- css text center property

<aside>

💡 **오늘의 추천 사이트**

</aside>

- caniuse.com

>CSS html 등등 여러 기술들 중에서 현재 웹 브라우저들이 얼마나 그 기술을  채택하고 있는지에 대한 통계를 보여주는 사이트임 아주 유용쓰 

---

<aside>

💬 **오늘 배운 정보**

</aside>

<웹페이지 안에 css를 삽입하는 방법>

1. style 태그를 쓴다
2. style 속성을 쓴다. 

밑줄을 만들거나 없애는 속성은 텍스트에 장식(꾸밈) 이 없다 (text decoration) 색깔은 hex code 입력해도 된다. 골라쓰자. 이왕 하는 거 예쁘게 살자.

```css

css의 ;(세미콜론) < 이거는 디스크립션이 시작하고 끝나는 것을 구분하기 위해 사용함. 만약에 세미콜론 없이 사용한다면,

color: red text-decoration :none 이렇게 되면 뭐가 뭔지 모르니까, 이것보단

color: red ;

text-decoration: none; 

이렇게 쓰는 게 훨씬 깔끔함. 

그러니까 무슨 효과를 지정하고 나면 꼭 ; (세미콜론) 사용할 것!

```

---

css에서 어떤 효과를 나타내는 것이 property(속성)  

align :정렬 

id 선택자와 class 선택자가 붙으면, id 선택자가 이긴다. 클래스 선택자와 태그 선택자가 붙으면 클래스 선택자가 이긴다. 태그선택자가 제일 약하다. 즉, 

* id 선택자>클래스 선택자>태그 선택자 

예를 들어 전부 같은 클래스 선택자라고 치면, 가장 마지막에 등장한 선택자가 가장 강하다. 그리고, id 값은 단 한번만 등장해야한다. id의 핵심은 중복되면 안된다 이다. 

element = 태그

h1 은 제목 태그인데, 화면 전체를 쓰는 것이 더 효율적이다. 링크 앞뒤에 있는 컨텐츠가 줄바꿈이 된다면 상당히 불편하기 때문에 기본적으로 링크는 줄바꿈을 하지 않고 딱 자기의 콘텐츠만큼의 크기를 쓴다. 

화면 전체를 쓰는 태그를 block 이라고 한다. 아닌 것을 inline 이라고 함. 이 block 과 inline 은 display라는 속성의 기본값일 뿐 css를 통해 언제든 바뀔 수 있다. 

display:none; 으로 써주면 안보이게도 할 수 있다.

,(콤마)라는 선택자로 코드의 양을 줄일 수 있다.

박스모델이 중요~ ! 검색하면 다나옴~! 모르겠으면 검색을 해보자.

- 그리드

디자인이라는 목적만을 위해 어떠한 의미도 존재하지 않는 태그를 사용해야 할 때가 있다. 그럴 때 사용하라고 하는 무색무취의 태그. div (division), div는 기본적으로 block level element이다. 즉 화면전체를사용한다.

같은 목적의 태그가 하나 더 있는데, span-inline, level element 이라고 한다, 목적에 따라 사용하면 된다.그리드를 사용하려면 각각의 태그의 부피감을 정확하게 알아야 할 필요가 있다. 태그 두개를 나란히 놓고자 한다면, 그리드를 사용할 때에 두 태그를 감싸는 부모가 필요하다. 

다음 부모태그에 id를 부여 

>태그를 배치할 때에는 그 태그를 감싸는 부모가 필요 

grid-template-columns(열): 150px 1fr

raw:행

>처음 거는 150픽셀로 하고, 그 다음거는 나머지 공간을 다 쓰겠다. 

둘다 1fr일때, 두개는 같은 크기가 된다. 

2fr 1fr 이면 전체가 2:1비율이 된다. 

---

gird는 최신 기술, 이런 것을 사용해도 되는지 아닌지를 판단해야 될 때 사이트를 활용하면 판단에 도움이 될 수 있다. 꼭 검색을 활용하자.

---

반응형 디자인을 responsible web 이라고 하는데, 화면의 크기에 따라서 웹페이지의 각 요소들이 반응하여 움직이게 된다.핵심적 요소는 미디어 쿼리. 

@media(min-width:800px){} 
> 이런 식으로 조건을 걸어서 css가 안보이고 보이게 할 수 있는 기능이다.

